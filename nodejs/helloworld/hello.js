console.log("hello world")

// console.log(window) // This line won't work in Node js Runtime because it can only be run from Browser!
                    // All the UI related work have been removed like navigator and all!
                    // So all the functions in the window package will not run! like alert and other things!

                    